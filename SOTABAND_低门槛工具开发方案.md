# SOTABand 低门槛工具开发方案

> 目标：让非计算机专业人员也能顺畅地完成工具开发与 Agent 搭建
> 日期：2026-08-30
> 状态：**第一阶段已完成并验证通过**（第二、三阶段待做）

## 实施状态速览

| 能力 | 状态 | 说明 |
|------|------|------|
| A. 分层摘要呈现 | ✅ **已完成** | 10/10 节点成功生成人话摘要 |
| B. 节点级精化 | ✅ **已完成** | 实测只改目标段落，其余 9 段内容不变 |
| 概览层 UI | ✅ **已完成** | 分组标题 + 缩进层次 + 一次点击看内容 |
| 改动对比与撤销 | ⬜ 待做 | 第二阶段 |
| 精化历史 | ⬜ 待做 | 第二阶段 |
| L2 粒度（参数行/步骤） | ⬜ 待做 | 第三阶段 |

**核心实测结论**（详见「六、实施经验」）：
- 精化相比整体重生成，Token 消耗低、可控性高、不会误改其他段落
- 使用者从"看内容"到"提完意见"的点击次数，经优化后从 6 次降到 3 次

---

## 一、背景与问题

### 1.1 老师的愿景

SOTABand 的定位是**面向跨学科人员的 AI 辅助开发工具**——使用者是各领域的
专业人员（如无人机应用、生物信息、材料科学），而非程序员。

这决定了产品设计的第一性原理：

> **使用者能判断"对不对"，但不应该被要求理解"怎么实现"。**

### 1.2 现状实测数据

对仓库中 36 份已生成工具文档的统计：

| 指标 | 数值 |
|------|------|
| 平均长度 | **2163 字符 / 87 行** |
| 最长文档 | 4674 字符 / 147 行 |
| 结构 | 固定 6 段（功能概述/输入/输出/依赖/运行机制/版本历史） |
| 表达形式 | 以 Markdown 表格为主，含 `integer` / `output_format` / `concurrency` 等技术术语 |

### 1.3 三个真实痛点

**痛点 1：文档太长，看不懂，无法判断"对不对"**

领域专家关心的是"这个工具能不能算出我要的结果"，但文档呈现的是参数类型、
输出格式、依赖库版本等技术细节。一份 87 行的表格文档，非专业人员很难
逐行核对，更遑论发现其中某处推断错误。

**痛点 2：改一处要重生成整篇**

立项时的能力矩阵：

| 能力 | 接口 | 当时状态 |
|------|------|---------|
| 生成文档 | `POST /generate-spec` | ✅ 整体生成 |
| 生成代码 | `POST /generate-code` | ✅ |
| 修改代码 | `POST /{id}/modify-code` | ✅ 自然语言改代码 |
| **修改文档** | —— | ❌ **缺失** |
| 保存文档 | `POST /{id}/save-spec` | ✅ 手动编辑后保存 |

**当时没有修改文档的接口**。使用者想改文档，只有两条路：
- 手动编辑 textarea（需要自己找到位置、写对格式）
- 回到 Step 1 重新生成整篇（之前的所有调整全部作废）

> **现状**：第一阶段已补齐（见「四、能力 B」），新增
> `POST /{id}/refine-spec-node`，支持只重写一个段落。

**痛点 3：整体重生成不稳定**

LLM 重写整篇文档时，即使只想改一个参数的默认值，其他段落也可能被顺带改动。
使用者面临"改一处、动全身"的不确定性。

---

## 二、方案总览

两个核心能力，解决上述三个痛点：

| 能力 | 解决的问题 | 对应痛点 |
|------|-----------|---------|
| **能力 A：分层摘要呈现** | 先给核心要点与结构树，再给技术细节 | 痛点 1 |
| **能力 B：节点级精化** | 点节点 → 说问题 → 只改那一处 | 痛点 2、3 |

---

## 三、能力 A：分层摘要呈现

### 3.1 设计思路

**双层呈现**：同一份规范，两种视图。

```
┌─ 概览层（默认展示，人话）────────────────┐
│ 这个工具做什么：                          │
│   对数据集里的航拍图片逐张打分，           │
│   评清晰度、光照、遮挡、完整性，           │
│   最后汇总成一份报告。                    │
│                                          │
│ 需要你提供：                              │
│   • 数据集名称  → 要评估哪批图片          │
│   • 同时处理几张（默认 8 张）              │
│                                          │
│ 会给你什么：                              │
│   • 一张表格，列出评分最低的 20 张        │
│   • 一份 JSON 报告文件                    │
│                                          │
│ 分几步完成：                              │
│   1. 找到数据集的图片                     │
│   2. 逐张调用大模型看图打分  ← 可点       │
│   3. 汇总统计并生成报告                   │
└──────────────────────────────────────────┘
              ↕ 一键切换
┌─ 细节层（技术文档，折叠）─────────────────┐
│ ## 2. 输入规范                            │
│ | 参数名 | 类型 | 必填 | 默认值 | 说明 |  │
│ | dataset_name | string | 是 | 无 | ... |  │
│ ...                                      │
└──────────────────────────────────────────┘
```

**关键设计**：概览层不是另一份文档，而是技术文档的**投影**——每个要点都锚定
到技术文档的对应节点。这样点击概览层的任一条目，就能定位到细节层的具体位置。

### 3.2 为什么这样有效

| 设计 | 解决的认知问题 |
|------|--------------|
| 默认展示概览层 | 降低首次认知负荷，使用者先建立整体印象 |
| 用"要评估哪批图片"代替"数据集名称参数" | 消除术语障碍，用人话描述业务含义 |
| 默认折叠技术细节 | 避免 87 行表格造成的信息过载 |
| 要点可点击定位 | 保留深入查看的通道，不剥夺控制权 |

### 3.3 摘要生成

由 LLM 在生成文档的同时产出，附加在文档元数据中：

```json
{
  "summary": {
    "one_liner": "对数据集中的航拍图片做四维度质量评估，输出报告",
    "what_it_does": [
      "对数据集里的航拍图片逐张打分",
      "评清晰度、光照、遮挡、完整性四个维度",
      "汇总成一份报告"
    ],
    "you_provide": [
      {"label": "数据集名称", "plain": "要评估哪批图片", "required": true},
      {"label": "同时处理几张", "plain": "一次同时评估几张图，默认 8 张", "required": false}
    ],
    "you_get": [
      "一张表格，列出评分最低的 20 张",
      "一份 JSON 报告文件"
    ]
  }
}
```

生成成本：约 300~500 token，与文档生成同批次完成，不额外增加请求。

### 3.4 摘要的耗时与缓存（重要）

**实测：生成一份文档的摘要需约 30 秒**（推理模型物理延迟，多种优化均无效）：

| 优化尝试 | 结果 |
|---------|------|
| 并发拆分（10 请求并行） | ❌ 无效，32.6s vs 33.7s |
| 减少输入长度（400→100 字） | ❌ 无效，波动范围内 |
| 降低 max_tokens | ❌ 无效 |
| prompt 加"要求简洁"（单节点） | ✅ 有效，12s→6.5s |

**因此摘要绝不能阻塞"查看概览"这个主流程。** 最终方案：

1. **概览默认只加载结构**（约 1ms，秒开）
2. **生成文档后后台预生成摘要**（`prefetchSummaries`，不阻塞界面）
3. **后端按文档内容哈希缓存**（TTL 1 小时）

实测缓存效果：

| | 耗时 |
|---|---|
| 首次（调模型） | 19,427 ms |
| 命中缓存 | **2 ms**（提速 9102x） |

> **为什么用内容哈希而非 tool_id**：新建阶段文档尚未落盘、没有稳定 id；
> 且文档被编辑后旧摘要即失效，内容哈希天然反映"摘要是否还适用"。

### 3.5 实测结果（已完成）

对「航拍图片批量评估」文档实测，**10/10 节点全部生成摘要成功**。质量对比：

| | 技术文档原文 | 生成的人话摘要 |
|---|---|---|
| 输入规范 | `concurrency \| integer \| 否 \| 8 \| 并发线程数...` | 需要提供数据集名称，以及可选的并发处理数量来控制同时分析的图片数 |
| 依赖环境 | `Pillow \| >=9.0 \| ...` | 工具依赖图片处理软件，版本要求和安装由系统自动管理，无需手动操作 |
| 标准输出字段 | `\| 字段 \| 类型 \| 说明 \|` | 工具会返回处理状态、结果说明和具体数据 |

**摘要中不含参数类型、字段名、库名**——这正是方案要的效果：领域专家看摘要就能判断"对不对"。

---

## 四、能力 B：节点级精化

### 4.1 节点树模型

MD 文档的 6 段骨架天然构成第一层节点。第二层下探到最常被修改的粒度：

```
工具规范文档
├─ 1. 功能概述                       [L1]
├─ 2. 输入规范                       [L1]
│   ├─ dataset_name                  [L2 参数行]
│   └─ concurrency                   [L2 参数行]
├─ 3. 输出规范                       [L1]
│   ├─ 3.1 标准输出字段              [L1]
│   │   └─ status / message / data   [L2 字段行]
│   └─ 3.2 可视化输出格式            [L1]
├─ 4. 依赖环境                       [L1]
│   └─ Pillow / numpy ...            [L2 依赖行]
├─ 5. 运行机制                       [L1]
│   ├─ 5.1 执行流程                  [L1]
│   │   ├─ 步骤1：获取图片列表        [L2 步骤]
│   │   ├─ 步骤2：并发调用大模型      [L2 步骤]
│   │   └─ 步骤3：汇总生成报告        [L2 步骤]
│   └─ 5. 2 错误处理                 [L1]
└─ 6. 版本历史                       [L1]
```

**第一版建议只做 L1（段落级）**，理由：
- 粒度足够粗，上下文完整，精化成功率高
- 实现简单（按 `## ` / `### ` 标题切分即可）
- 验证可行性后再下探到 L2

### 4.2 数据结构

```python
@dataclass
class SpecNode:
    id: str              # "sec-2-input" / "sec-5-1-flow"
    title: str           # "输入规范"
    level: int           # 1 = 段落, 2 = 子项
    node_type: str       # section | param | step | output_field | dependency
    summary: str         # 人话摘要（LLM 生成）
    content_md: str      # 原始 MD 片段
    line_start: int      # 在原文中的起始行
    line_end: int        # 在原文中的结束行
    children: list[str]  # 子节点 id

@dataclass
class SpecOutline:
    tool_id: str
    nodes: list[SpecNode]
    summary: dict        # 概览层内容
    raw_md: str          # 完整文档
    version: int         # 精化次数，用于提示"已改过几次"
```

### 4.3 精化流程

```
使用者                          系统
  │
  ├─ 点击节点「输入规范」────────►  高亮该节点的 MD 片段
  │
  ├─ 点「这里有问题」────────────►  弹出输入框（预填该节点摘要）
  │
  ├─ 输入「并发数默认改成 4」────►  POST /refine-spec-node
  │                                  │
  │                                  ├─ 取出该节点 content_md
  │                                  ├─ 拼装 prompt（只含该节点 + 少量上下文）
  │                                  ├─ LLM 返回新的节点片段
  │                                  ├─ 校验：片段能解析、标题层级未变
  │                                  └─ 替换回原文，重建节点树
  │
  ├─ ◄── 展示改动对比（改前 / 改后）
  │
  └─ 确认 / 撤销
```

### 4.4 精化 Prompt 设计

关键点：**只喂该节点，不是整篇文档**。

```python
prompt = f"""你正在修改一份工具规范文档的**某一个段落**。

【整体背景】（只读，用于保持一致性，不要修改这些内容）
{其他节点的标题与摘要，不含正文}

【当前要修改的段落】
{node.content_md}

【使用者的问题】
{user_feedback}

【要求】
1. 只输出修改后的**这一个段落**，不要输出整篇文档
2. 保持原有的 Markdown 结构与标题层级
3. 不要改动使用者没提到的内容
4. 如果修改会影响到其他段落，在末尾用一行注释说明：
   <!-- 影响提示: xxx -->

修改后的段落："""
```

**为什么"其他节点只给标题和摘要"**：既保证 LLM 理解整体语境（避免改出
不一致的内容），又避免它顺手改动其他段落。

### 4.5 接口设计

| 接口 | 方法 | 状态 | 用途 |
|------|------|------|------|
| `/api/tool/{id}/spec-outline` | POST | ✅ 已实现 | 解析文档，返回节点树 + 可选摘要 |
| `/api/tool/{id}/refine-spec-node` | POST | ✅ 已实现 | 精化单个节点 |

> **实现与原规划的两点差异**：
> 1. `spec-outline` 用 POST 而非 GET——需传 `spec_md` 正文，GET 不适合带正文
> 2. **支持占位 tool_id（如 `_draft_`）—— 新建工具未注册时也能用概览与精化，
>    仅不能落盘。** 因为概览/精化本身不依赖注册信息，限制它毫无必要
>    （初版曾错误地要求必须先注册，导致新建流程看不到概览）

`refine-spec-node` 请求/响应：

```jsonc
// 请求（实际字段）
{
  "node_id": "h2-输入规范",        // 节点 id 来自 spec-outline
  "feedback": "并发数默认值改成 4，并说明小于1时按4处理",
  "spec_md": "完整文档；留空则用已保存的文档",
  "save": false                    // 是否落盘；占位 id 时强制为 false
}

// 响应（实际字段）
{
  "node_id": "h2-输入规范",
  "node_md": "修改后的该段落",
  "updated_md": "替换后的完整文档",
  "outline": { /* 重建后的节点树 */ },
  "diff": { "before": "原段落", "after": "新段落" },
  "impact_hint": "该改动可能影响「5.1 执行流程」中的并发处理逻辑",
  "saved": false
}
```

### 4.6 实测结果（已完成）

用真实文档做端到端测试：

| 检查项 | 结果 |
|--------|------|
| 精化后变更的节点数 | **1/10**（只改目标段落，其余 9 段内容完全一致） |
| 节点 id 稳定性 | ✅ 精化前后 id 不变（前端展开/选中状态不会错乱） |
| 越界防护 | ✅ 模型若返回其他段落，返回 422 阻止替换 |
| 影响提示 | ✅ 改输入规范时自动提示"可能影响错误处理或执行流程" |
| 错误处理 | ✅ 未知节点 404、空反馈 400、未知工具 404 |

**精化示例**（反馈："并发数默认值改成 4，并说明小于1时按4处理"）：

```
改后表格行：
| concurrency | integer | 否 | 4 | 并发线程数... |
说明：默认值4。小于1时按4处理...

影响提示：该改动可能影响「5.1 执行流程」中的并发处理逻辑
```

---

## 五、实施计划

### 第一阶段：基础能力 —— ✅ **已完成（2026-08-30）**

| # | 任务 | 状态 | 交付物 |
|---|------|------|--------|
| 1 | MD 结构化解析器 | ✅ | `core/resource/spec_outline.py` |
| 2 | 节点摘要生成 | ✅ | 集成在 `spec-outline`（`with_summary=true`） |
| 3 | 概览层 UI | ✅ | `SpecOutlineView.tsx` + 接入 Step2 |
| 4 | `refine-spec-node` 接口 | ✅ | `app/api/routes/tool_routes.py` |

产出：使用者能看到人话摘要，能点击段落精化。**最小闭环已打通。**

### 第二阶段：体验完善（3 项）

| # | 任务 | 说明 |
|---|------|------|
| 5 | 改动对比与撤销 | 精化后展示 diff，支持一键回退 |
| 6 | 影响范围提示 | 改输入规范时提示"可能影响执行流程" |
| 7 | 精化历史 | 记录每次精化，可查看"改过什么" |

### 第三阶段：粒度下探（2 项）

| # | 任务 | 说明 |
|---|------|------|
| 8 | L2 节点支持 | 参数行、流程步骤也可单独精化 |
| 9 | 节点级代码联动 | 精化文档节点后，提示"代码需要重新生成" |

---

## 六、实施经验（第一阶段的坑与解法）

> 这一节记录实际开发中踩到的问题。**复现成本高的坑尤其值得记录**——
> 它们不报错、不崩溃，只是"效果不对"，极难排查。

### 6.0 坑 1：推理模型的 max_tokens 必须给足（**高频**）

**现象**：摘要生成 10/10 全部为空，无任何报错。

**根因**：要求 LLM 输出 JSON，给了 `max_tokens=2000`。该模型是推理模型，
会先输出大量思考 token —— **思考阶段就耗尽额度，返回空字符串**，
且只在末尾附一句 `[WARNING] Response truncated`。这不是异常，
`try/except` 完全捕获不到。

**这是我第二次踩同一个坑**（上一次是工具代码里的 `max_tokens`：
实测 1500 不够、3000 才够）。两次都是"静默返回空"。

**解法**：
1. 推理模型的 `max_tokens` 一律给足（摘要场景用 8000）
2. 显式检测 `[WARNING]` 标记，判定为截断
3. **绝不用 `except: pass` 静默吞掉失败** —— 把原因写到 `outline.warning`，
   否则前端只会看到一片空白，无从排查

**通用原则**：LLM 返回空不等于失败，需要主动判空 + 检测截断标记。

### 6.1 坑 2：不要要求 LLM 复现系统生成的 ID

**现象**：让 LLM 输出 `{节点id: 摘要}` 的 JSON，结果全部匹配不上。

**根因**：节点 id 是系统生成的（如 `h2-gong-neng-gai-shu`），
要求模型准确复现这种人工构造的字符串，写错概率很高。

**解法**：改为**按编号顺序输出数组** —— 模型只写摘要，不复现 id，
由系统按序对应。既更稳，输出也更短。

**通用原则**：让模型输出"它擅长的内容"，不要用 ID、哈希等人工构造的
标识去为难它。

### 6.2 坑 3：把"某能力的前置条件"错误地扩大到整个组件

**现象**：使用者在新建工具流程中完全看不到概览层。

**根因**：我写了 `{isEditMode && <OutlineSection/>}`，而
`isEditMode = Boolean(editingToolId)`。精化接口确实需要 toolId，
但我把这个约束套到了整个概览组件上 —— 而**概览本身只需要文档内容**。

**解法**：概览无条件显示；只有需要落盘时才检查 toolId。
并引入占位 id `_draft_` 支持未注册文档（仅不能落盘）。

**通用原则**：定位清楚"哪个子功能真正依赖某条件"，不要图省事一刀切。

### 6.3 坑 4：交互路径过长（使用者反馈发现）

**现象**：要看某段原文，需「点箭头 → 点查看原文 → 看到内容」三步；
且点完箭头只出现一个按钮，什么内容都没有 —— 这一步等于白点。

**解法**：
- 整行可点击（扩大点击区），而非只有小箭头
- 展开后**直接显示原文**，去掉二次点击
- 未展开时也显示预览（按内容形态提取：表格取首列、列表去符号）
- 精化输入框自动聚焦 + `⌘/Ctrl+Enter` 提交

效果：**从"看内容"到"提完意见"的点击次数从 6 次降到 3 次**。

**反思**：自己写的代码，操作路径熟，感受不到繁琐 ——
**这类问题只有真实使用者才能暴露**。

### 6.4 坑 5：纯容器节点被当作普通卡片展示

**现象**：`## 3. 输出规范` 这类节点（只有标题、正文全在子节点里）
被渲染成卡片，展开后只有一行标题，毫无信息量。

**解法**：识别「有子节点 + 正文为空」的容器节点，渲染为**分组标题**，
子节点缩进其下。同时去掉子标题的章节编号（`3.1 xxx` → `xxx`），
因为层次已由缩进体现。

### 6.5 坑 6：样式对齐需要手算偏移

**现象**：分组标题的类型标签（"给什么"）比卡片内的标签靠左 31px，
视觉上射出去了。

**根因**：卡片有 `border(1) + padding(10) + 箭头(12) + gap(8) = 31px`，
分组标题是 0。

**解法**：给分组标题补上相同的 `px-2.5` + `w-3` 占位箭头。

### 6.6 坑 7：慢操作不该阻塞主流程，且必须缓存

**现象**：点「文档要点概览」要等 33.7 秒才有反应。

**根因**：概览展开时同步生成人话摘要（调推理模型，单节点 6-7 秒 × 10 节点）。

**解法**（三层）：
1. 概览默认只加载结构（1ms），摘要改为按需/后台
2. 生成文档后**后台预生成**摘要（`prefetchSummaries`，不设 `isGenerating`，
   不阻塞界面交互）
3. 后端按**文档内容哈希**缓存（首次 19.4s → 命中 2ms）

**注意**：不能把摘要并入 `generate-spec` 同步执行——
生成文档本身需 27.5 秒，再加 30 秒会变成 57 秒，产生卡死感。

### 6.7 坑 8：模型返回的数组长度可能不足

**现象**：要求模型输出 10 项摘要的 JSON 数组，实测有时只返回 6 项，
缺失节点静默留空。

**根因**：长文档下模型输出被截断或自行精简。

**解法**：
- 检测缺失项，对**缺失的节点单独补一次**（只补缺失的，不重跑全部——
  每次调用 30 秒，全量重跑代价太高）
- 全部为空时不写缓存，避免把失败结果固化 1 小时

---

## 七、技术要点与风险

### 7.1 要点 1：节点 ID 的稳定性

精化后文档重建，节点 ID 必须保持稳定，否则前端的展开状态、选中状态会错乱。

**方案**：ID 由「层级 + 标题哈希」生成，标题不变则 ID 不变。
若使用者改了标题，ID 变化是可接受的（视为新节点）。

### 7.2 要点 2：精化后的一致性校验

LLM 可能返回越界内容（改了输入规范，却把输出规范也返回了）。

**方案**：
- 校验片段不含其他节点的标题（出现则说明越界，返回 422 拒绝替换）
- 用原文行范围做替换（而非字符串查找，避免相同文本导致错替换）

### 7.3 要点 3：成本与稳定性对比

| 方式 | Token 消耗 | 稳定性 | 可控性 |
|------|-----------|--------|--------|
| 整体重新生成 | 高（全篇 2000+ 字符） | 低（可能改动无关段落） | 低 |
| **节点精化** | **低（单段约 300 字符）** | **高（上下文小，聚焦）** | **高（改前改后可见）** |

节点精化在三个维度上全面优于整体重生成——这正是本方案的核心价值。
实测佐证：精化后变更节点数 1/10，其余段落内容完全一致。

### 7.4 风险：概览层摘要与细节层不同步

若使用者手动编辑了技术文档，概览层摘要会过时。

**缓解**：
- 前端已实现：文档内容变化即丢弃缓存的节点树，需重新解析
- 手动编辑保存后，提示"摘要可能需要更新"，提供一键重新生成

### 7.5 风险：非专业使用者描述不清问题

"这里不对"这类反馈，LLM 难以理解。

**缓解**：
- 输入框给出引导式提示（"例如：把并发数的默认值改成 4"）— 已实现
- 精化失败时，给出澄清性追问而非直接报错 — 待做

---

## 八、为什么这个方向是对的

### 8.1 符合使用者的心智模型

领域专家的认知方式是**自上而下**：先理解整体目标，再逐层深入细节。
原系统强迫他们自下而上（先读 87 行表格，再拼凑出整体印象），这是错配。

### 8.2 把"判断"和"实现"分离

| 谁负责 | 内容 |
|--------|------|
| **使用者** | 判断"这个工具是不是我想要的"（概览层） |
| **系统** | 处理"怎么实现"（细节层 + 代码） |

使用者只需在概览层做判断，在发现问题时用自然语言描述——这正是他们擅长的。

### 8.3 降低试错成本

工具开发的本质是迭代。迭代成本从"重生成整篇 + 重新检查 87 行"，
降到"改一个段落 + 看一处 diff"，试错意愿和效率都会显著提升。

### 8.4 已被实测验证

| 判断 | 实测结果 |
|------|---------|
| 摘要能让非专业人员看懂 | ✅ 10/10 无参数类型/字段名/库名 |
| 节点精化比整体重生成更可控 | ✅ 变更 1/10，其余段落不变 |
| 交互能被接受 | ✅ 经 3 轮反馈优化后使用者基本满意 |

---

## 九、附：已复用的基础设施

本方案未从零开始，以下既有能力被复用：

| 已有能力 | 复用点 |
|---------|--------|
| MD 固定 6 段模板 | 节点树的天然骨架，无需定义新格式 |
| `modify-code` 接口 | 精化流程的参考（prompt 结构、代码块清洗） |
| `save-spec` 接口 | 精化结果的落盘 |
| 编辑器 Step2 的 MD 编辑框 | 细节层 UI 的基础 |
| `core/security/secrets.py` | 精化时的异常脱敏 |

---

## 十、下一步建议

按使用者实际反馈的优先级排（而非工作量）：

1. **先收集真实使用反馈** —— 当前版本已可用，让老师/同学实际用一轮，
   看摘要是否真的易懂、交互是否还有别扭处
2. 第二阶段：diff 可视化优化、精化历史、失败时的澄清性追问
3. 第三阶段：L2 粒度、节点与代码联动

**不建议**在没有真实反馈前继续加功能——本轮实践已证明，
自己写的代码、自己检查，看不出交互繁琐和视觉错位这类问题。
