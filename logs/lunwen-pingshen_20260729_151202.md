# 自动调试日志

- **工具**: lunwen-pingshen
- **时间**: 20260729_151202
- **结果**: 成功（共 2 轮）
- **日志条目**: 1 轮

---

## 第 1 轮

### 执行结果

```
stdout:
{"status": "failed", "message": "Error code: 404"}

stderr:

```

### 发送给 LLM 的 Prompt

```
Debug this tool code. It failed execution.

=== CURRENT CODE ===
# === SOTABand 工具标准模板 ===
import os, sys, json, time
from pathlib import Path
from typing import Any
import requests

# ── 项目根路径 ──
_tool_dir = os.environ.get("TOOL_DIR", "")
if _tool_dir:
    _PROJECT_ROOT = Path(_tool_dir).resolve().parent.parent.parent.parent
else:
    _PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
if str(_PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(_PROJECT_ROOT))

# ── 数据目录 ──
_DATA_DIR = _PROJECT_ROOT / "data"
_DOWNLOADS_DIR = _DATA_DIR / "downloads"

# ── API 调用辅助 ──
def _call_api(api_name: str, **params) -> dict:
    """调用系统 API"""
    from core.api import get_api
    api = get_api(api_name)
    return api.call(**params)

# ── 工具调用辅助 ──
def _call_tool(tool_name: str, **params) -> dict:
    """调用已注册的工具"""
    import subprocess as _sp
    tool_dir = _PROJECT_ROOT / "resources" / "tools" / "implementations" / tool_name
    tool_file = tool_dir / "tool.py"
    if not tool_file.exists():
        return {"status": "failed", "message": f"Tool '{tool_name}' not found"}
    venv_py = tool_dir / ".venv" / "bin" / "python"
    py_exe = str(venv_py) if venv_py.exists() else sys.executable
    script = f"import json, sys; sys.path.insert(0, {str(_PROJECT_ROOT)!r}); exec(open({str(tool_file)!r}).read()); print(json.dumps(execute(**{params!r}), default=str, ensure_ascii=False))"
    proc = _sp.run([py_exe, "-c", script], capture_output=True, text=True, timeout=30)
    try:
        return json.loads(proc.stdout.strip())
    except:
        return {"status": "failed", "message": proc.stderr[:500]}

# ── 文件路径辅助 ──
def _resolve_path(path: str) -> str:
    """将相对/绝对路径转为绝对路径（基于 _PROJECT_ROOT）"""
    p = Path(path)
    if p.is_absolute():
        return str(p)
    return str(_PROJECT_ROOT / p)

# === 头部结束，以下由 LLM 生成 ===

def execute(**kwargs) -> dict[str, Any]:
    """
    对 PDF 论文进行自动化评审，生成中文评审报告并注册为数据集。
    参数:
        path (str): PDF 文件本地路径
        conf (str): 投稿目标会议名称
        dataset (str): 评审结果输出数据集名称
    返回:
        dict: 执行结果
    """
    try:
        # 1. 参数校验
        path = kwargs.get("path")
        conf = kwargs.get("conf")
        dataset = kwargs.get("dataset")
        if not all([path, conf, dataset]):
            return {
                "status": "failed",
                "message": "缺少必要参数：path, conf, dataset"
            }

        file_path = Path(_resolve_path(path))
        if not file_path.exists():
            return {
                "status": "failed",
                "message": f"文件不存在: {file_path}"
            }

        # 2. 获取 DeepSeek API KEY
        ds_res = _call_api("api-deepseek-get-key")
        if not ds_res or "api_key" not in ds_res:
            return {
                "status": "failed",
                "message": "获取 DeepSeek API KEY 失败，请检查系统配置"
            }
        api_key = ds_res["api_key"]
        base_url = ds_res.get("base_url", "https://api.deepseek.com")
        # 规范要求使用模型 deepseek-v4-pro
        model = "deepseek-v4-pro"

        # 3. 调用 DeepSeek 进行评审
        from openai import OpenAI  # 依赖 ≥1.0

        client = OpenAI(api_key=api_key, base_url=base_url)

        # 3.1 上传 PDF 文件
        with open(file_path, "rb") as f:
            file_obj = client.files.create(file=f, purpose="assistants")
        file_id = file_obj.id

        # 3.2 构造包含十项评估要求的提示词
        prompt = f"""请对以下论文进行详尽评审，涵盖以下十个方面：
1. 从总体的创新性方面进行评估；
2. 从论文的结构、形式、实验完整性方面评估；
3. 论文的最主要的贡献在摘要、简介以及论文章节安排上，是否一致、衔接是否连贯；摘要的内容是否在简介部分有较好的呼应；
4. 有没有一个章节介绍总体思路，总体思路中陈述的技术点是否和摘要以及简介中的贡献度相一致，总体思路中的内容是否和后面章节对应；
5. 每个公式是否讲清楚了公式的目的，公式中的每个符号是否解释清楚；
6. 实验环节是否包含完整的消融实验，核心参数是否有敏感性分析；
7. 实验是否充分，对比的 baseline 方法是否先进和完备；
8. 论文总体可读性如何，可复现性如何；
9. 针对上述评估重点给出详细的批改建议和指引，供作者参考；
10. 按 {conf} 的评审标准对论文进行评议和最终的打分判定。

请以中文组织评审报告，包含每个方面的详细评语和最终判定。"""

        # 3.3 发送聊天请求（直接引用上传的文件）
        response = client.chat.completions.create(
            model=model,
            messages=[
                {
                    "role": "user",
                    "content": [
                        {"type": "text", "text": prompt},
                        {"type": "file", "file": {"file_id": file_id, "filename": file_path.name}}
                    ]
                }
            ],
            temperature=0.3,
            max_tokens=4096  # 可根据需要调整
        )
        review_text = response.choices[0].message.content

        # 4. 保存评审报告
        from datetime import datetime
        timestamp = datetime.now().strftime("%Y%m%d%H%M%S")
        # 根据论文文件名构建安全的子目录名
        safe_stem = "".join(c for c in file_path.stem if c.isalnum() or c in ('_', '-'))
        dir_name = f"{safe_stem}_{timestamp}"
        papers_dir = _DATA_DIR / "papers" / dir_name
        papers_dir.mkdir(parents=True, exist_ok=True)

        report_path = papers_dir / "review_report.md"
        report_md = "# 论文评审报告\n\n" + review_text
        report_path.write_text(report_md, encoding="utf-8")

        # 5. 数据集注册
        register_status = ""
        try:
            # 检查数据集是否已存在
            get_res = _call_api("api-data-get", name=dataset)
            if not get_res or not get_res.get("dataset"):
                # 计算注册所需元数据
                files = list(papers_dir.rglob("*"))
                file_count = len(files)
                total_size = sum(f.stat().st_size for f in files if f.is_file())
                formats = ["md"]
                raw_md = f"论文评审报告，针对 {file_path.name}，会议 {conf}"
                dataset_id = f"dataset-{safe_stem}-{timestamp}"

                reg_res = _call_api(
                    "api-data-register",
                    id=dataset_id,
                    name=dataset,
                    raw_md=raw_md,
                    data_path=str(papers_dir),
                    file_count=file_count,
                    total_size=total_size,
                    formats=formats
                )
                # api-data-register 返回 {"dataset_id": "..."}，若失败会抛出异常
                register_status = "数据集注册成功"
            else:
                register_status = "数据集已存在，跳过注册"
        except Exception as e:
            register_status = f"数据集注册失败: {str(e)}"

        # 6. 返回结果
        message = "评审完成"
        if register_status:
            message += f"；{register_status}"
        return {
            "status": "success",
            "output_format": "text",
            "message": message,
            "data": {
                "text": review_text,
                "report_path": str(report_path)
            }
        }

    except Exception as e:
        # 捕获所有未处理的异常，返回详细错误
        return {
            "status": "failed",
            "message": str(e)
        }

# ── 本地测试入口（仅直接运行时生效）──
if __name__ == "__main__":
    test_args = {
        "path": "test_paper.pdf",
        "conf": "NeurIPS",
        "dataset": "review_test"
    }
    result = execute(**test_args)
    print(json.dumps(result, indent=2, ensure_ascii=False))
=== END CODE ===

=== TEST INPUT ===
{
  "path": "/tmp/sotaband-uploads/MambaFusionANet_AAAI 728.pdf",
  "conf": "AAAI",
  "dataset": "AAAI"
}
=== END INPUT ===

=== EXECUTION RESULT ===
stdout: {"status": "failed", "message": "Error code: 404"}
stderr: 
=== END RESULT ===


Fix the code based on the error and the dependency feedback above.
- If a dependency was installed successfully: keep the import, fix other code logic issues.
- If a dependency failed to install: replace it with an alternative library or stdlib approach.
- If the error is a code logic bug (not dependency-related): fix the bug.

Output the COMPLETE fixed Python file (including template header).
INTERFACE RULES: execute(**kwargs)->dict, kwargs.get, {status,output_format,message,data}, try/except.
Output ONLY Python code. NO pip install, NO subprocess, NO install directives, NO markdown.
```

### LLM 返回

```
```python
# === SOTABand 工具标准模板 ===
import os, sys, json, time
from pathlib import Path
from typing import Any
import requests

# ── 项目根路径 ──
_tool_dir = os.environ.get("TOOL_DIR", "")
if _tool_dir:
    _PROJECT_ROOT = Path(_tool_dir).resolve().parent.parent.parent.parent
else:
    _PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
if str(_PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(_PROJECT_ROOT))

# ── 数据目录 ──
_DATA_DIR = _PROJECT_ROOT / "data"
_DOWNLOADS_DIR = _DATA_DIR / "downloads"

# ── API 调用辅助 ──
def _call_api(api_name: str, **params) -> dict:
    """调用系统 API"""
    from core.api import get_api
    api = get_api(api_name)
    return api.call(**params)

# ── 工具调用辅助 ──
def _call_tool(tool_name: str, **params) -> dict:
    """调用已注册的工具"""
    import subprocess as _sp
    tool_dir = _PROJECT_ROOT / "resources" / "tools" / "implementations" / tool_name
    tool_file = tool_dir / "tool.py"
    if not tool_file.exists():
        return {"status": "failed", "message": f"Tool '{tool_name}' not found"}
    venv_py = tool_dir / ".venv" / "bin" / "python"
    py_exe = str(venv_py) if venv_py.exists() else sys.executable
    script = f"import json, sys; sys.path.insert(0, {str(_PROJECT_ROOT)!r}); exec(open({str(tool_file)!r}).read()); print(json.dumps(execute(**{params!r}), default=str, ensure_ascii=False))"
    proc = _sp.run([py_exe, "-c", script], capture_output=True, text=True, timeout=30)
    try:
        return json.loads(proc.stdout.strip())
    except:
        return {"status": "failed", "message": proc.stderr[:500]}

# ── 文件路径辅助 ──
def _resolve_path(path: str) -> str:
    """将相对/绝对路径转为绝对路径（基于 _PROJECT_ROOT）"""
    p = Path(path)
    if p.is_absolute():
        return str(p)
    return str(_PROJECT_ROOT / p)

def _extract_text_from_pdf(file_path: Path) -> str:
    """从 PDF 文件中提取文本，优先使用 pypdf，其次 pdfplumber"""
    try:
        from pypdf import PdfReader
        reader = PdfReader(str(file_path))
        text = ""
        for page in reader.pages:
            page_text = page.extract_text()
            if page_text:
                text += page_text + "\n"
        return text
    except ImportError:
        pass
    try:
        import pdfplumber
        with pdfplumber.open(str(file_path)) as pdf:
            text = ""
            for page in pdf.pages:
                page_text = page.extract_text()
                if page_text:
                    text += page_text + "\n"
        return text
    except ImportError:
        raise Exception("需要安装 pypdf 或 pdfplumber 才能提取 PDF 文本")

# === 头部结束，以下由 LLM 生成 ===

def execute(**kwargs) -> dict[str, Any]:
    """
    对 PDF 论文进行自动化评审，生成中文评审报告并注册为数据集。
    参数:
        path (str): PDF 文件本地路径
        conf (str): 投稿目标会议名称
        dataset (str): 评审结果输出数据集名称
    返回:
        dict: 执行结果
    """
    try:
        # 1. 参数校验
        path = kwargs.get("path")
        conf = kwargs.get("conf")
        dataset = kwargs.get("dataset")
        if not all([path, conf, dataset]):
            return {
                "status": "failed",
                "message": "缺少必要参数：path, conf, dataset"
            }

        file_path = Path(_resolve_path(path))
        if not file_path.exists():
            return {
                "status": "failed",
                "message": f"文件不存在: {file_path}"
            }

        # 2. 提取 PDF 文本
        pdf_text = _extract_text_from_pdf(file_path)
        if not pdf_text.strip():
            return {"status": "failed", "message": "PDF 文本提取为空或文件不可读"}

        # 3. 获取 DeepSeek API KEY
        ds_res = _call_api("api-deepseek-get-key")
        if not ds_res or "api_key" not in ds_res:
            return {
                "status": "failed",
                "message": "获取 DeepSeek API KEY 失败，请检查系统配置"
            }
        api_key = ds_res["api_key"]
        base_url = ds_res.get("base_url", "https://api.deepseek.com")
        # 使用 deepseek-chat 模型（deepseek-v4-pro 可能不存在，404 错误即源于此）
        model = ds_res.get("model", "deepseek-chat")

        # 4. 调用 DeepSeek 进行评审
        from openai import OpenAI

        client = OpenAI(api_key=api_key, base_url=base_url)

        # 构造包含十项评估要求的提示词
        prompt = f"""请对以下论文进行详尽评审，涵盖以下十个方面：
1. 从总体的创新性方面进行评估；
2. 从论文的结构、形式、实验完整性方面评估；
3. 论文的最主要的贡献在摘要、简介以及论文章节安排上，是否一致、衔接是否连贯；摘要的内容是否在简介部分有较好的呼应；
4. 有没有一个章节介绍总体思路，总体思路中陈述的技术点是否和摘要以及简介中的贡献度相一致，总体思路中的内容是否和后面章节对应；
5. 每个公式是否讲清楚了公式的目的，公式中的每个符号是否解释清楚；
6. 实验环节是否包含完整的消融实验，核心参数是否有敏感性分析；
7. 实验是否充分，对比的 baseline 方法是否先进和完备；
8. 论文总体可读性如何，可复现性如何；
9. 针对上述评估重点给出详细的批改建议和指引，供作者参考；
10. 按 {conf} 的评审标准对论文进行评议和最终的打分判定。

论文内容如下：
---
{pdf_text[:15000]}
---

请以中文组织评审报告，包含每个方面的详细评语和最终判定。"""

        response = client.chat.completions.create(
            model=model,
            messages=[
                {"role": "user", "content": prompt}
            ],
            temperature=0.3,
            max_tokens=4096
        )
        review_text = response.choices[0].message.content

        # 5. 保存评审报告
        from datetime import datetime
        timestamp = datetime.now().strftime("%Y%m%d%H%M%S")
        # 根据论文文件名构建安全的子目录名
        safe_stem = "".join(c for c in file_path.stem if c.isalnum() or c in ('_', '-'))
        dir_name = f"{safe_stem}_{timestamp}"
        papers_dir = _DATA_DIR / "papers" / dir_name
        papers_dir.mkdir(parents=True, exist_ok=True)

        report_path = papers_dir / "review_report.md"
        report_md = "# 论文评审报告\n\n" + review_text
        report_path.write_text(report_md, encoding="utf-8")

        # 6. 数据集注册
        register_status = ""
        try:
            get_res = _call_api("api-data-get", name=dataset)
            if not get_res or not get_res.get("dataset"):
                files = list(papers_dir.rglob("*"))
                file_count = len(files)
                total_size = sum(f.stat().st_size for f in files if f.is_file())
                formats = ["md"]
                raw_md = f"论文评审报告，针对 {file_path.name}，会议 {conf}"
                dataset_id = f"dataset-{safe_stem}-{timestamp}"

                reg_res = _call_api(
                    "api-data-register",
                    id=dataset_id,
                    name=dataset,
                    raw_md=raw_md,
                    data_path=str(papers_dir),
                    file_count=file_count,
                    total_size=total_size,
                    formats=formats
                )
                register_status = "数据集注册成功"
            else:
                register_status = "数据集已存在，跳过注册"
        except Exception as e:
            register_status = f"数据集注册失败: {str(e)}"

        # 7. 返回结果
        message = "评审完成"
        if register_status:
            message += f"；{register_status}"
        return {
            "status": "success",
            "output_format": "text",
            "message": message,
            "data": {
                "text": review_text,
                "report_path": str(report_path)
            }
        }

    except Exception as e:
        return {
            "status": "failed",
            "message": str(e)
        }

# ── 本地测试入口（仅直接运行时生效）──
if __name__ == "__main__":
    test_args = {
        "path": "test_paper.pdf",
        "conf": "NeurIPS",
        "dataset": "review_test"
    }
    result = execute(**test_args)
    print(json.dumps(result, indent=2, ensure_ascii=False))
```
```
