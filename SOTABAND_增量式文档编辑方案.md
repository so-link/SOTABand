# SOTABand 增量式文档编辑方案

> 问题：改一个默认值却要花和全量重生成相近的时间，很不优雅
> 日期：2026-08-30
> 依据：业界调研（Aider / Anthropic / AdaEdit / 受监管行业 SOP 编辑实践）+ 本地实测

---

## 一、结论先行

真正的问题不是"精化不够快"，而是**把所有改动都交给了 LLM**。

正确架构（业界共识）：

> **LLM 提议，系统应用，人类批准。**
> —— 且**不是所有改动都需要 LLM 参与**。

拆解后：

| 改动类型 | 占比 | 正确做法 | 耗时 |
|---------|------|---------|------|
| **改参数值/类型/必填** | 高 | **表单直接编辑**（程序化改表格） | **0 ms** |
| 补充说明、改措辞 | 中 | LLM 精化 | ~11 s |
| 结构性调整（加参数、改流程） | 低 | LLM 精化 + 人工确认 | ~11 s |

**大部分"改一点点"其实属于第一类，根本不需要调 LLM。**

---

## 二、为什么当前实现不够优雅

### 2.1 实测数据

| 操作 | 耗时 |
|------|------|
| 解析文档为节点树 | 1 ms |
| **精化 1 个节点** | **11,455 ms**（中位） |
| 全量重生成文档 | 38,763 ms |

精化已比全量快 70%，但 11 秒对"改一个默认值"仍然过慢——因为**只要调用 LLM 就是 10 秒级**（推理模型思考开销）。

### 2.2 根因：所有改动都走 LLM

当前只有一条路径：

```
用户说"并发数改成4" → 整个段落进 LLM → LLM 重写整个段落 → 替换
```

即使只改一个字符，也要付全额的思考成本。

---

## 三、业界调研结论

### 3.1 编辑格式的选择（Aider / Anthropic / AdaEdit）

| 格式 | 适用 | 说明 |
|------|------|------|
| Full rewrite | **文件 < 300 tokens** | 短内容下锚点开销反而超过重写 |
| Search-replace | 文件 > 300 tokens | AdaEdit 实测**降 30%+ 延迟与成本** |
| 行号 unified diff | 不推荐 | "LLM 不擅长处理行号"（Aider 文档） |

**关键**：Aider 把 strict unified diff 改成 search-replace 后，GPT-4 得分从 26% 升到 59%，**未换模型、未换 prompt**。

**但本地实测**：330 个节点中**仅 2%（7 个）超过 300 tokens 阈值**。
→ 自适应格式只影响 2% 的场景，**是次要优化，不该优先做**。

### 3.2 Search-replace 的必要条件：锚点唯一

Anthropic 的 `str_replace` 工具**要求匹配唯一**，多处匹配时报错并强制模型扩展上下文。

我此前的实测失败正是缺了这一步——模型输出的 `old` 片段（如 `| 否 | 8 |`）
在原文中可能出现多次，直接字符串替换会误伤。

### 3.3 受监管行业（制药/航空）的核心原则

> **LLM 永远不应持有或输出完整的可变文档。**

默认行为是危险的：LLM 会改同义词（`shall`→`must`）、调语序、加过渡句——
即使你只让它改一个值。

共识架构：

```
LLM 提议（结构化 patch） → 系统确定性应用 → 人类批准
```

并配套：
- **温度 0.0**（原文用词：non-negotiable）
- **哈希校验**：未请求修改的段落，改前后哈希必须一致
- **few-shot 示例**：展示"坏编辑"（改了整段语气）vs"好编辑"（只改一个值），
  比抽象指令有效得多

---

## 四、改进方案（按优先级）

### P0：表单化直接编辑（**零延迟**，解决主要痛点）

**思路**：输入/输出规范的表格是结构化的，可程序化解析，不需要 LLM。

**已验证可行性**：

```
表头: ['参数名', '类型', '必填', '默认值', '说明']
可定位: dataset_name  默认值=无
可定位: concurrency   默认值=8     ← 可直接改这一格
```

**交互设计**：

```
┌ 输入规范 ───────────────────────────┐
│ 参数名        类型      必填  默认值 │
│ dataset_name  string   是    无    │  [编辑]
│ concurrency   integer  否    8     │  [编辑]  ← 点这里
└────────────────────────────────────┘
                    ↓ 点击
┌ 编辑参数 concurrency ──────────────┐
│ 类型   : [integer      ▾]          │
│ 必填   : [否           ▾]          │
│ 默认值 : [4            ]           │  ← 直接改
│ 说明   : [并发线程数...           ] │
│                                    │
│        [取消]  [保存]              │
└────────────────────────────────────┘
```

**实现要点**：
1. 解析 Markdown 表格 → 结构化行（表头+数据）
2. 修改指定单元格 → 重新渲染表格 → 按行范围替换回文档
3. **不调用 LLM，0 延迟，100% 精确**

**适用**：输入规范、输出规范、依赖环境等表格型节点。
实测这些节点占多数，能覆盖大部分"改一点点"的场景。

### P1：精化时应用最佳实践 —— ✅ **已完成（2026-08-30）**

| 项 | 改前 | 改后 | 依据 |
|---|------|------|------|
| temperature | 0.2 | **0.0** | 文档编辑要求确定性（原文：non-negotiable） |
| few-shot | 无 | **加"坏/好编辑"对照示例** | 比抽象指令更有效 |
| 哈希校验 | 弱越界检测 | **`_check_untouched_intact()`：未修改节点内容必须逐字一致** | 防止隐性改动 |
| 提示词 | "只改使用者指出的问题" | **"你是保守的编辑，不是作者" + 明确列出 5 类禁止行为** | 对抗 LLM 的"改写欲" |

**提示词中的禁止行为清单**（实测有效）：
```
禁止：改同义词 / 调整语序 / 补充过渡句润色 /
     修改表格中未提及的单元格 / 调整格式
即使原文表达不完美，也不要顺手改进 —— 使用者没有要求。
```

**实测效果**（反馈："并发数默认值改成 4"）：

```
-| concurrency | integer | 否 | 8 | 并发线程数…若传入值类型非整数或小于1，则使用默认值8。 |
+| concurrency | integer | 否 | 4 | 并发线程数…若传入值类型非整数或小于1，则使用默认值4。 |
差异行数 2 —— 只改了目标行，其余 9 个节点内容哈希完全一致
```

#### 附带实现：值一致性提醒

「最小改动」策略有个副作用：模型严格只改被要求之处，
**不会顺带同步其他位置对同一值的引用**。例如默认值 8→4 后，
说明列可能仍写着"小于 1 时使用默认值 8"。

因此加了 `_check_value_consistency()`：
- 检测同行其他单元格是否仍引用旧值
- 不阻止修改，只在 `impact_hint` 中提醒
- 只对 ≤16 字符的短值检测（长文本误报率高）

实测三种情形：

| 情形 | 结果 |
|------|------|
| 说明未同步 | ✅ 提醒「8」已改为「4」，但同行中仍出现「8」 |
| 说明已同步 | ✅ 不提醒（无噪音） |
| 长文本改动 | ✅ 不检测（避免误报） |

### P2：自适应编辑格式（影响面小，架构正确）

节点 > 300 tokens 时用 search-replace，**并加锚点唯一性校验**：

```python
def apply_patch(md: str, old: str, new: str) -> str:
    count = md.count(old)
    if count == 0:
        raise ValueError("锚点在原文中不存在")
    if count > 1:
        raise ValueError(f"锚点匹配 {count} 处，不唯一，请扩展上下文")
    return md.replace(old, new, 1)
```

本地仅 2% 节点适用，但大段落（如"5.1 执行流程"，实测最长 3113 字符）能受益。

---

## 五、补充：为什么不该继续优化"精化速度"

我此前尝试过多种提速，均无效：

| 尝试 | 结果 |
|------|------|
| 并发拆分 | ❌ 无效（32.6s vs 33.7s） |
| 减少输入长度（400→100 字） | ❌ 无效，在波动内 |
| 降低 max_tokens | ❌ 无效 |
| 只输出改动片段 | ❌ **反而慢 3 倍**（小节点下 full rewrite 本就更便宜） |

**根因**：推理模型的耗时由**思考量**决定，与输入输出长度关系不大。
只要调用 LLM，就是 10 秒级——这是模型的物理特性，不是实现问题。

**因此正确的方向不是"让 LLM 更快"，而是"让更多改动不需要 LLM"。**

---

## 六、任务分级：为什么改代码快、改文档慢（**核心发现**）

### 6.1 你的直觉是对的：业界确实这么做

Claude Code 用**三个正交机制**控制推理深度：

| 机制 | 取值 | 关键细节 |
|------|------|---------|
| **Effort** | `low/medium/high/max` | **默认 medium 而非 high** —— "大多数编程交互不需要最深推理" |
| **Thinking** | `adaptive/enabled/disabled` | **`disabled` 专用于"API key 验证等低开销调用"** |
| **Fast Mode** | 路由到不同模型 | 带冷却状态机，限流自动回退 |

**Ultrathink**：用户消息里写 `ultrathink` 关键词，自动把 effort 从 medium 提升到 high
—— **零摩擦升级路径**，不用离开对话流改设置。

> 来源：《驾驭工程——从 Claude Code 源码到 AI 编码最佳实践》第 21 章
> https://zhanghandong.github.io/harness-engineering-from-cc-to-ai-coding/part6/ch21.html

### 6.2 但本项目的端点不支持这些参数

实测 mimo-v2.5（`token-plan-cn` 端点）：

| 参数 | 结果 |
|------|------|
| `temperature=0.0` | ✅ 有效，**快 47%**（2377→1257 ms） |
| `reasoning_effort=low` | ⚠️ 被接受但**慢 59%** |
| `reasoning_effort=minimal` | ❌ 400 错误 |
| `thinking=disabled` | ❌ SDK 不支持该参数 |
| `extra_body.reasoning_effort` | ⚠️ 被接受但**慢 90%** |

**结论**：该端点是 **adaptive thinking**（模型自行决定思考量），
不提供手动的推理预算开关。**无法照搬 Claude Code 的 effort 机制。**

### 6.3 那"改代码快"的真正原因是什么 —— 分离实验

三档对比（各 3 次取中位，趋势单调、噪声小）：

| 配置 | 耗时 | 倍数 |
|------|------|------|
| A 短prompt + **短内容（改几行代码）** | 2,394 ms | 1.0x |
| B 短prompt + 文档段落 | 10,896 ms | **4.6x** |
| C 完整prompt + 文档段落（原实现） | 21,295 ms | **8.9x** |

**答案：Claude Code 快，是因为它改的是"A 档"（代码片段 2.4 秒），
而本项目原实现在"C 档"（文档段落 + 长 prompt 21 秒）。**

两个主因都被量化：

| 因素 | 影响 |
|------|------|
| **内容复杂度**（代码 → 文档段落） | **4.6x** |
| **prompt 复杂度**（few-shot + 其他节点上下文） | **2.0x** |

**我加的 few-shot 和"其他节点上下文"让耗时翻倍了** ——
这是为质量付出的代价，但不必对所有改动都付。

### 6.4 落地：fast / full 分级（已实现）

参照 Claude Code"默认 medium、按需升级"的哲学：

```
默认 fast（精简 prompt，约 11 秒）
    │
    ├─ 校验通过 → 返回（mode=fast）
    └─ 校验失败（越界/哈希不一致/空返回）
           ↓
       升级 full（含 few-shot + 上下文，约 21 秒）重试一次
```

**实测**：11.3s / 6.4s，均为 `mode=fast`（原实现 21.3s），**快约 2 倍**，
且质量有哈希校验兜底。

| 档位 | prompt 内容 | 耗时 | 适用 |
|------|------------|------|------|
| `fast` | 精简指令，无 few-shot / 无其他节点上下文 | ~11 s | 默认 |
| `full` | few-shot 示例 + 其他节点上下文 | ~21 s | fast 校验失败时自动升级 |

> 流式接口同样支持，升级时推送 `retry` 事件，前端可显示"正在用更严格的方式重试"。

---

## 七、等待反馈优化（已完成）

### 5.1 为什么需要

实测该模型**思考阶段占绝大部分时间**：

```
首个 token: 12,480 ms
done 事件:  12,480 ms   ← 几乎同时到达
```

即：思考 12.5 秒，生成瞬间完成（36 个 token 一次性涌出）。
这意味着**流式无法产生"逐字显示"效果**，但等待期间若无任何反馈，
使用者会认为系统卡住。

### 5.2 实现

| 层 | 改动 |
|---|------|
| 后端 | 新增 `POST /{id}/refine-spec-node-stream`（SSE：begin / token / done / error） |
| 后端 | 抽出 `_build_refine_prompt()` 与 `_postprocess_refined()`，流式与非流式**共用**，避免两份逻辑漂移 |
| 前端 | `refineSpecNodeStream()` 解析 SSE 帧并回调 `onToken` |
| 前端 | **计时器**：每秒更新，显示"模型思考中…已等待 N 秒（复杂改动可能需要 15~30 秒）" |
| 前端 | 生成阶段实时显示已收到内容 |

### 5.3 关键认知

> **流式的价值不在于"逐字显示"（该模型做不到），而在于"证明系统在做事"。**

计时器 + 预期时长提示（"可能需要 15~30 秒"）是改善体验的主力：
使用者知道要等多久，就不会反复点击或以为崩溃。

---

## 八、一个尚未解决的测量问题

本地延迟**噪声极大**（相同配置重复测量，波动 4.5s ~ 16.2s，变异系数 > 40%）。

这意味着：
- 单次或少次测量得出的"优化效果"不可信
- 我曾据此得出过错误结论（以为减少输出能提速，实测反而更慢）

**后续做性能优化时，必须先做大样本测量**（建议 ≥ 10 次取中位数），
或改用客观指标（如 token 消耗）而非端到端耗时。

---

## 九、实施建议

| 优先级 | 任务 | 价值 | 状态 |
|-------|------|------|------|
| **P0** | 表格行解析 + 表单化编辑 | **零延迟**（0.83ms vs 11s） | ✅ 已完成 |
| **P1** | 精化最佳实践（温度/few-shot/哈希/提示词） | 提升可靠性与精确度 | ✅ 已完成 |
| P2 | 自适应格式 + 锚点唯一性校验 | 影响 2% 节点 | ⬜ 待做 |

**P0 + P1 已完成**，核心诉求已解决：

| 改动类型 | 路径 | 耗时 |
|---------|------|------|
| 改参数值/类型/必填/依赖版本 | **点单元格直接改** | **0.83 ms** |
| 补充说明、改措辞、结构调整 | LLM 精化（已应用最佳实践） | ~11 s |

P2（自适应编辑格式）影响面仅 2%，建议等有实际大段落编辑需求时再做。

---

## 十、参考资料

- **Edit Format Selection: Diff vs Search/Replace vs Full Rewrite**（AgentPatterns）
  https://www.agentpatterns.ai/tool-engineering/llm-edit-format-selection/
  多个独立来源验证；Aider 实测 unified diff → search-replace 使 GPT-4 得分 26%→59%

- **The LLM Must Never Hold the Pen: Surgical SOP Editing**（Saram Consulting）
  https://saram.io/blog/surgical-sop-editing-llms-2026/
  受监管行业的增量编辑架构；"LLM 提议、系统应用、人类批准"

- **第21章：Effort、Fast Mode 与 Thinking**（《驾驭工程——从 Claude Code 源码到 AI 编码最佳实践》）
  https://zhanghandong.github.io/harness-engineering-from-cc-to-ai-coding/part6/ch21.html
  Claude Code 三个推理控制机制的源码级分析；effort 分级、Ultrathink 关键词触发、
  adaptive thinking 的模型兼容性分层

- **How should AI coding agents decide between "edit-in-place" and "rewrite-from-scratch"**（MoltQ）
  https://moltq.chat/questions/5610b5b5-975c-4106-bd9a-fe5db1730163/
  给出可强制执行的分类规则：changed_lines ≤ 80 且 ≤ 15% 文件、
  functions_touched ≤ 2、hunks ≤ 3 时用 edit-in-place；
  file_change_ratio ≥ 0.35 或 functions ≥ 5 时改用 rewrite

- 相关论文：
  - Cao et al., "To Diff or Not to Diff?", arxiv:2604.27296（AdaEdit）
  - Glukhov et al., Diff-XYZ, arxiv:2510.12487
