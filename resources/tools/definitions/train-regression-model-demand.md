输入: CSV格式的数据文件{path}, 回归器类型{model_type} （如随机森林、xgboost、线性回归等），测试集占比{r}
过程：
(1) 从指定路径{path}加载CSV文件，按 (1-r):r 的比例划分训练集和测试集，训练回归模型{model_type} ，根据其他列（除了第1列）的数值预测最后1列的数值，训练好的模型文件的路径为{model}
(2)调用【模型注册API】注册模型{model}

输出：模型{model}的路径，训练的误差，训练的准确率。