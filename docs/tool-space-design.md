# SOTABand 工具空间前端布局设计文档 v2.0

---

## 目录

1. [概述](#一概述)
2. [架构设计](#二架构设计)
3. [页面布局](#三页面布局)
4. [工具仓库浏览页](#四工具仓库浏览页)
5. [交互流程](#五交互流程)
6. [标签系统](#六标签系统)
7. [API 接口](#七api-接口)
8. [前端状态管理](#八前端状态管理)
9. [数据模型](#九数据模型)
10. [实施计划](#十实施计划)

---

## 一、概述

### 1.1 设计目标

将当前的工具管理从「平铺列表」升级为「仓库 + 工作空间」双层架构，支持工具的规模化管理和按需加载。

### 1.2 核心概念

| 概念 | 定义 | 存储位置 | 类比 |
|------|------|---------|------|
| **工具仓库** | 系统中所有已创建工具的全局注册中心，可删除 | `resources/tools/registry.json` + `implementations/` | npm registry |
| **工具空间** | 当前工作区加载的工具子集，按需从仓库导入 | 前端 zustand store | `package.json` dependencies |
| **工具标签** | 分类筛选元数据，创建时 LLM 自动生成 + 用户可修改 | registry.json 中 tools[].tags | npm keywords |

### 1.3 三者关系

```
工具仓库 (全部工具)
    │
    │ 新建工具 ──→ 自动加入工具空间
    │ 从仓库导入 ──→ 加入工具空间
    │
    ▼
工具空间 (用户加载的工具子集)
    │
    │ 从空间移除 ──→ 不影响仓库
    │
    ▼
工具标签 (分类体系)
    └── 创建时 LLM 自动生成
    └── 用户可手动增删改
    └── 用于仓库浏览页筛选
```

---

## 二、架构设计

### 2.1 工具生命周期

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   创建工具                                               │
│   (ToolEditorView: 描述→MD→代码→调试→注册)               │
│      │                                                  │
│      ├──→ ① 注册到工具仓库 (registry.json)              │
│      │       · 保存 spec.md 到 definitions/             │
│      │       · 保存 tool.py 到 implementations/         │
│      │       · LLM 自动生成标签                          │
│      │                                                  │
│      └──→ ② 自动加入工具空间 (前端 store)               │
│                                                         │
│   工具在空间中:                                          │
│      · 用户点击查看详情/执行                              │
│      · 用户可从空间移除 (不影响仓库)                      │
│      · 用户可在仓库页删除 (同时从空间移除)                │
│                                                         │
│   工具在仓库中:                                          │
│      · 可被搜索/标签筛选发现                              │
│      · 可被任何工作空间导入                               │
│      · 可被删除 (彻底清除)                               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 状态流转

```
                    ┌──────────┐
                    │  创建工具  │
                    └────┬─────┘
                         │
                         ▼
              ┌─────────────────────┐
              │   工具仓库 (已注册)   │
              └─────────┬───────────┘
                        │
            ┌───────────┴───────────┐
            │                       │
            ▼                       ▼
   ┌──────────────┐        ┌──────────────┐
   │ 工具空间 (已加载)│        │ 仓库浏览页     │
   │ · 查看详情     │        │ · 浏览/搜索    │
   │ · 执行工具     │        │ · 标签筛选    │
   │ · 从空间移除   │        │ · 导入到空间   │
   └──────┬───────┘        │ · 删除工具     │
          │                └──────┬───────┘
          │                       │
          ▼                       ▼
   从空间移除 (仅状态)      从仓库删除 (彻底清除
   不影响仓库                + 从空间自动移除)
```

---

## 三、页面布局

### 3.1 整体三栏结构

```
┌──────────────────────────────────────────────────────────────┐
│  TopBar (Logo + 全局导航)                                     │
├──────────┬───────────────────────────────────┬───────────────┤
│          │                                   │               │
│  工具空间 │        Center Panel               │  右侧属性面板  │
│  左边栏   │    (根据选中工具动态切换)           │  (可收起)     │
│          │                                   │               │
│ ┌──────┐ │  ┌─ 工具详情 (ToolDetailView)      │               │
│ │📦仓库 │ │  │  查看工具信息、执行工具          │               │
│ │ 入口  │ │  ├─ 工具编辑 (ToolEditorView)      │               │
│ └──────┘ │  │  创建新工具                     │               │
│          │  └─ 仓库浏览 (RepositoryView)       │               │
│ 已加载   │     浏览全部工具、搜索筛选           │               │
│ 工具 (3) │                                   │               │
│ ┌──────┐ │                                   │               │
│ │tool-1│ │                                   │               │
│ │tool-2│ │                                   │               │
│ │tool-3│ │                                   │               │
│ └──────┘ │                                   │               │
│          │                                   │               │
│ [+添加]  │                                   │               │
│          │                                   │               │
├──────────┴───────────────────────────────────┴───────────────┤
│  StatusBar                                                    │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 工具空间左边栏

```
┌──────────────────────────┐
│ 🔧 工具空间               │
│ ──────────────────────── │
│                          │
│ ┌──────────────────────┐ │
│ │ 📦 工具仓库           │ │  ← 点击进入仓库浏览页
│ │   浏览全部工具   →    │ │     (切换到 RepositoryView)
│ └──────────────────────┘ │
│                          │
│ 已加载工具 (3)            │  ← 分区标题 + 计数
│ ──────────────────────── │
│                          │
│ ┌──────────────────────┐ │
│ │ 📄 paper-summary      │ │  ← 点击查看详情 (ToolDetailView)
│ │   论文摘要             │ │
│ │   #LLM  #文档         │ │  ← 标签小圆点
│ │                [×]   │ │  ← 从空间移除
│ └──────────────────────┘ │
│                          │
│ ┌──────────────────────┐ │
│ │ 🖼️ image-resize       │ │
│ │   图片缩放             │ │
│ │   #图像  #处理        │ │
│ │                [×]   │ │
│ └──────────────────────┘ │
│                          │
│ ┌──────────────────────┐ │
│ │ 📊 csv-analyze        │ │
│ │   CSV分析             │ │
│ │   #数据  #表格        │ │
│ │                [×]   │ │
│ └──────────────────────┘ │
│                          │
│ ──────────────────────── │
│ [+ 添加工具]              │  ← 点击弹出选择弹窗
│                          │
└──────────────────────────┘
```

**工具项交互**：
- **单击** → 在 Center Panel 打开工具详情页 (ToolDetailView)
- **× 按钮** → 从工作空间移除（不删除仓库中的工具）
- **悬停** → 显示完整名称和标签

### 3.3 添加工具弹窗

```
┌───────────────────────────────────┐
│  添加工具                          │
│ ───────────────────────────────── │
│                                   │
│  ┌─────────────────────────────┐ │
│  │ 📦 从工具仓库导入      →    │ │  ← 打开仓库浏览页选择
│  │   浏览已有工具并添加到空间   │ │
│  └─────────────────────────────┘ │
│                                   │
│  ┌─────────────────────────────┐ │
│  │ 🆕 创建新工具          →    │ │  ← 进入工具编辑器
│  │   自然语言描述，自动生成代码  │ │
│  └─────────────────────────────┘ │
│                                   │
│  ┌─────────────────────────────┐ │
│  │ 📋 从代码导入          →    │ │  ← 粘贴或上传 Python 代码
│  │   已有代码快速注册为工具     │ │
│  └─────────────────────────────┘ │
│                                   │
└───────────────────────────────────┘
```

---

## 四、工具仓库浏览页

### 4.1 页面布局

```
┌──────────────────────────────────────────────────────────────┐
│  ← 返回空间    📦 工具仓库 (共 47 个工具)                      │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ 🔍 搜索工具...                  [名称▼] [标签▼] [最新▼]  ││
│  └──────────────────────────────────────────────────────────┘│
│                                                              │
│  标签筛选:                                                    │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐  │
│  │ ✓ 全部  │ │ #LLM   │ │ #图像   │ │ #数据   │ │ + 更多...│  │
│  │  (47)   │ │  (12)  │ │  (8)   │ │  (15)  │ │          │  │
│  └────────┘ └────────┘ └────────┘ └────────┘ └──────────┘  │
│                                                              │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐    │
│  │📄 paper-   │ │🖼️ image-   │ │📊 csv-     │ │🔊 audio-   │    │
│  │ summary    │ │ resize     │ │ analyze    │ │ convert    │    │
│  │ 论文摘要   │ │ 图片缩放   │ │ CSV分析    │ │ 音频转换   │    │
│  │ #LLM #文档 │ │ #图像 #处理 │ │ #数据 #表格 │ │ #音频 #转换 │    │
│  │ v1.2·23次  │ │ v2.0·8次   │ │ v1.0·15次  │ │ v0.9·3次   │    │
│  │            │ │            │ │            │ │            │    │
│  │[🗑️] [+添加]│ │[🗑️] [✓已加载]│ │[🗑️] [+添加]│ │[🗑️] [✓已加载]│    │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘    │
│                                                              │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐    │
│  │ ...        │ │ ...        │ │ ...        │ │ ...        │    │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘    │
│                                                              │
│  ──────────────────────────────────────────────────────────  │
│  < 1  2  3  4  5  >    每页 [16▼] 个                         │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 工具卡片详细设计

```
┌──────────────────────────┐
│                          │
│  📄 paper-summary         │  ← 图标 + 工具ID (monospace)
│  论文摘要工具              │  ← 中文名称 (bold)
│                          │
│  #LLM    #文档    #PDF    │  ← 标签 (点击可单独筛选该标签)
│                          │
│  v1.2  ·  已使用 23 次    │  ← 版本号 + 使用统计
│                          │
│  ┌────────┐ ┌──────────┐ │
│  │ 🗑️ 删除 │ │ [+ 添加] │ │  ← 操作按钮
│  └────────┘ └──────────┘ │
│                          │
└──────────────────────────┘
```

**按钮状态说明**：

| 状态 | 添加按钮 | 删除按钮 |
|------|---------|---------|
| 未加载到空间 | `[+ 添加]` 蓝色可点击 | `[🗑️ 删除]` 红色可点击 |
| 已加载到空间 | `[✓ 已加载]` 灰色不可点击 | `[🗑️ 删除]` 红色可点击 |

**卡片交互**：
- 点击卡片主体 → 进入工具详情页
- 点击标签 → 触发该标签的筛选
- 点击 `[+ 添加]` → 导入到工作空间
- 点击 `[🗑️ 删除]` → 弹出确认框后删除

### 4.3 删除工具确认弹窗

```
┌───────────────────────────────────┐
│  ⚠️ 确认删除                       │
│ ───────────────────────────────── │
│                                   │
│  确定要删除工具 "paper-summary" 吗？│
│                                   │
│  此操作不可撤销，将永久删除：        │
│  · 工具代码文件                    │
│  · 工具规范文档                    │
│  · 注册记录                       │
│                                   │
│  该工具将从所有工作空间中移除。      │
│                                   │
│  ┌──────────┐ ┌────────┐          │
│  │  取消     │ │ 确认删除 │          │
│  └──────────┘ └────────┘          │
│                                   │
└───────────────────────────────────┘
```

### 4.4 标签筛选

- **多选逻辑**：点击多个标签取交集（AND），显示同时拥有所有选中标签的工具
- **"全部"按钮**：清除所有筛选，显示全部工具
- **"更多"按钮**：当标签超过一行时，点击展开全部标签
- **标签统计**：每个标签旁 `(N)` 表示该标签下的工具数量
- **点击卡片上的标签**：等同于点击顶部筛选栏中的对应标签
- **选中标签高亮**：选中状态有视觉区分

### 4.5 搜索与排序

| 排序方式 | 说明 | 默认 |
|---------|------|------|
| 按名称 | 工具 ID 字母序 (A-Z) | |
| 按标签 | 同标签工具聚合展示 | |
| 最新 | 按 created_at 倒序 | ✓ |
| 最常用 | 按 usage_count 倒序 | |

### 4.6 分页

- 默认每页 16 个工具卡片
- 下拉可选择 8 / 16 / 24 / 32 / 48
- 4 列网格布局
- 底部页码导航

---

## 五、交互流程

### 5.1 从仓库导入工具

```
用户在工具空间左边栏点击 [+ 添加]
  → 弹出选择弹窗
  → 选择 "从工具仓库导入"
  → 切换到仓库浏览页 (RepositoryView)
  → 浏览/搜索/标签筛选找到目标工具
  → 点击工具卡片上的 [+ 添加]
  → 工具添加到左边栏 "已加载工具" 列表
  → 按钮变为 [✓ 已加载]
  → 用户可继续浏览或返回空间
```

### 5.2 创建新工具

```
用户在工具空间左边栏点击 [+ 添加]
  → 弹出选择弹窗
  → 选择 "创建新工具"
  → 切换到工具编辑器 (ToolEditorView)
  → 填写需求描述 → 生成 MD 规范 → 生成代码 → 自动调试 → 注册
  → 注册成功后:
      ① 工具写入仓库 (registry.json + 文件)
      ② LLM 自动生成标签
      ③ 工具自动加入当前工具空间左边栏
      ④ 切换到工具详情页
```

### 5.3 从代码导入

```
用户在工具空间左边栏点击 [+ 添加]
  → 弹出选择弹窗
  → 选择 "从代码导入"
  → 弹出代码输入框
  → 粘贴 Python 代码 或 上传 .py 文件
  → 系统分析代码:
      · 提取函数签名和参数
      · 提取 import 依赖
      · 生成 MD 规范文档
  → 用户确认后注册:
      ① 写入仓库
      ② LLM 打标签
      ③ 加入工具空间
```

### 5.4 从工作空间移除工具

```
用户在左边栏工具项上点击 [×]
  → 工具从左边栏 "已加载工具" 列表中移除
  → 仓库中该工具不受影响
  → 可随时从仓库重新导入
```

### 5.5 从仓库删除工具

```
用户在仓库浏览页工具卡片上点击 [🗑️ 删除]
  → 弹出确认弹窗
  → 用户点击 "确认删除"
  → 调用 DELETE /api/tool/{id}
  → 后端删除:
      · resources/tools/registry.json 中的记录
      · resources/tools/implementations/{id}/ 整个目录
      · resources/tools/definitions/{id}.md
  → 前端:
      · 仓库列表移除该工具
      · 如果该工具已加载到工作空间，自动从空间移除
      · 刷新标签统计
```

### 5.6 查看工具详情

```
用户在左边栏点击工具项
  → 切换到工具详情页 (ToolDetailView)
  → 显示: 工具信息、代码、参数、执行按钮
```

---

## 六、标签系统

### 6.1 自动标签生成

工具注册时，LLM 根据工具信息自动生成标签。

**调用时机**：`POST /api/tool/register` 成功后，异步调用 LLM

**Prompt**：

```
根据以下工具信息，生成3-5个简短的中文标签（每个2-4字），
用于工具分类和检索。标签应准确描述工具的核心功能和适用场景。
返回 JSON 数组，不要其他内容。

工具名称: {name}
工具描述: {spec_md[:500]}

返回格式: ["标签1", "标签2", "标签3"]
```

**示例**：

| 工具 | 生成的标签 |
|------|----------|
| paper-summary | `["LLM", "文档", "PDF", "摘要"]` |
| image-resize | `["图像", "处理", "缩放", "格式转换"]` |
| csv-analyze | `["数据", "表格", "分析", "统计"]` |

### 6.2 手动标签编辑

- **位置**：工具详情页 (ToolDetailView) 标签区域
- **删除标签**：点击标签上的 × 图标
- **添加标签**：在输入框中输入新标签，回车确认
- **保存**：修改后自动保存到 registry.json
- **API**：`POST /api/tool/{id}/tags` body: `{"tags": ["标签1", "标签2"]}`

### 6.3 标签聚合

前端加载仓库时：
1. 遍历所有工具的 `tags` 字段
2. 去重排序
3. 统计每个标签下的工具数量
4. 按工具数量降序排列（常用标签在前）

---

## 七、API 接口

| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| `GET` | `/api/tool/list` | 获取仓库所有工具列表 | 已有 |
| `POST` | `/api/tool/register` | 注册新工具 | 已有 |
| `DELETE` | `/api/tool/{id}` | 删除工具（registry + 文件） | 已有 |
| `POST` | `/api/tool/{id}/tags` | 更新工具标签 | **新增** |
| `GET` | `/api/tool/repository` | 仓库工具（含标签统计） | **新增** |

### 7.1 `POST /api/tool/{id}/tags`

```json
// Request
{ "tags": ["LLM", "文档", "PDF", "摘要"] }

// Response
{ "status": "ok", "tags": ["LLM", "文档", "PDF", "摘要"] }
```

### 7.2 `GET /api/tool/repository`

```json
// Response
{
  "tools": [
    {
      "id": "paper-summary",
      "name": "论文摘要工具",
      "version": "1.2.0",
      "tags": ["LLM", "文档", "PDF"],
      "usage_count": 23,
      "created_at": "2026-07-26T10:00:00Z"
    }
  ],
  "tag_stats": {
    "LLM": 12,
    "图像": 8,
    "数据": 15,
    "文档": 6
  },
  "total": 47
}
```

---

## 八、前端状态管理

### 8.1 工具空间 Store (`workspace-tool-store.ts`)

```typescript
interface WorkspaceTool {
  id: string
  name: string
  tags: string[]
  loadedAt: string
}

interface WorkspaceToolState {
  tools: WorkspaceTool[]
  addTool: (tool: WorkspaceTool) => void
  removeTool: (id: string) => void
  isLoaded: (id: string) => boolean
}
```

### 8.2 仓库浏览 Store (`repository-store.ts`)

```typescript
interface RepositoryTool {
  id: string
  name: string
  version: string
  tags: string[]
  usage_count: number
  created_at: string
}

interface TagStat {
  tag: string
  count: number
}

interface RepositoryState {
  tools: RepositoryTool[]
  tagStats: TagStat[]
  selectedTags: string[]
  searchQuery: string
  sortBy: 'name' | 'latest' | 'usage'
  page: number
  pageSize: number
  loading: boolean

  fetchTools: () => Promise<void>
  toggleTag: (tag: string) => void
  setSearch: (query: string) => void
  setSort: (sort: 'name' | 'latest' | 'usage') => void
  setPage: (page: number) => void
  importToWorkspace: (tool: RepositoryTool) => void
  deleteTool: (id: string) => Promise<void>
}
```

### 8.3 组件树

```
CenterPanel
  ├── ToolSpaceView (新增：工具空间主视图)
  │     ├── 左边栏 (已加载工具列表 + 添加入口)
  │     └── 右边:
  │           ├── ToolDetailView (工具详情)
  │           ├── ToolEditorView (创建/编辑工具)
  │           └── RepositoryView (新增：仓库浏览页)
  │
  ├── RepositoryView (新增：工具仓库浏览)
  │     ├── 搜索栏
  │     ├── 标签筛选栏
  │     ├── 工具卡片网格
  │     └── 分页控件
  │
  ├── ToolDetailView (已有，需增强：标签编辑)
  │
  └── ToolEditorView (已有，需增强：注册后自动加入空间)
```

---

## 九、数据模型

### 9.1 工具仓库条目（registry.json）

```json
{
  "id": "paper-summary",
  "name": "论文摘要工具",
  "version": "1.2.0",
  "type": "function",
  "language": "python",
  "status": "active",
  "spec_path": "definitions/paper-summary.md",
  "impl_path": "implementations/paper-summary/",
  "input_schema": {},
  "output_schema": {},
  "param_meta": [],
  "tags": ["LLM", "文档", "PDF"],
  "usage_count": 23,
  "created_at": "2026-07-26T10:00:00Z"
}
```

**新增/修改字段**：
- `tags` — 已有，确保存在
- `usage_count` — 已有

### 9.2 工具空间列表（前端 store）

```typescript
interface WorkspaceTool {
  id: string        // 工具ID
  name: string      // 工具名称
  tags: string[]    // 标签列表
  loadedAt: string  // 加载到空间的时间
}
```

---

## 十、实施计划

### Phase 1 — 工具仓库浏览页（核心）

**目标**：用户可以浏览仓库中所有工具，按标签筛选，搜索，分页

**新增文件**：
- `frontend/src/components/center-panel/RepositoryView.tsx` — 仓库浏览页
- `frontend/src/stores/repository-store.ts` — 仓库状态管理

**修改文件**：
- `frontend/src/components/center-panel/CenterPanel.tsx` — 添加 `repository` 路由
- `frontend/src/stores/ui-store.ts` — 添加 `repository` 视图类型
- `app/api/routes/tool_routes.py` — 新增 `/repository` 端点

**功能**：
- 工具卡片 4 列网格展示
- 标签筛选栏（多选取交集）
- 搜索框
- 排序下拉（名称/最新/最常用）
- 分页控件
- 删除按钮 + 确认弹窗

### Phase 2 — 工具空间左边栏重构（核心）

**目标**：左边栏改为 "已加载工具" 列表 + 仓库入口 + 添加弹窗

**新增文件**：
- `frontend/src/components/center-panel/ToolSpaceView.tsx` — 工具空间主视图
- `frontend/src/stores/workspace-tool-store.ts` — 工作空间状态管理

**修改文件**：
- `frontend/src/components/center-panel/CenterPanel.tsx` — 添加 `tool-space` 路由
- `frontend/src/components/left-panel/ResourceBrowser.tsx` — 工具列表改为从工作空间 store 读取

**功能**：
- 左边栏显示已加载工具
- 仓库入口按钮
- 添加工具弹窗（三个选项）
- 从空间移除工具

### Phase 3 — 导入 + 新建自动加入（核心）

**目标**：从仓库导入工具到空间；新建工具自动加入空间

**修改文件**：
- `frontend/src/stores/repository-store.ts` — 添加 `importToWorkspace`
- `frontend/src/stores/tool-editor-store.ts` — 注册成功后自动加入空间
- `frontend/src/components/center-panel/RepositoryView.tsx` — 添加按钮状态联动

**功能**：
- 仓库页 [+ 添加] 按钮功能
- 已加载工具显示 [✓ 已加载]
- 新建工具注册后自动加入空间

### Phase 4 — 标签系统（增强）

**目标**：LLM 自动打标签 + 用户手动编辑

**新增文件**：
- `app/api/routes/tool_routes.py` — 新增 `/tags` 端点

**修改文件**：
- `core/resource/builder/tool_builder.py` — 注册时异步调 LLM 打标签
- `frontend/src/components/center-panel/ToolDetailView.tsx` — 标签编辑 UI

**功能**：
- 注册时 LLM 自动生成 3-5 个标签
- 工具详情页可编辑标签（增删）
- 标签自动保存

### Phase 5 — 从代码导入（增强）

**目标**：粘贴 Python 代码快速注册为工具

**功能**：
- 代码输入框（粘贴或上传 .py 文件）
- 自动分析函数签名和参数
- 生成 MD 规范文档
- 注册到仓库 + 加入空间

---

**总计**：3 个核心阶段 + 2 个增强阶段。Phase 1-3 完成后即可上线使用。
