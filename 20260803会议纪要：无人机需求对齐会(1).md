# 20260803会议纪要：无人机需求对齐会

**时间**：2026-08-03

---

## 一、四大核心功能

### 功能一：线索智能化生成

**输入**（业务方提供）：

- 带坐标图集：每张图片需含**时间 + 空间（经纬度）** 元数据（摄像机为固定安装位置、无人机为实时经纬度），**全景影像、无人机补拍图等所有图片来源均需带坐标**。图片来源：事件触发瞬间抓拍图、事件前一周（可配置）历史回溯图、事件后一周（可配置）持续跟踪图、覆盖事件前后时间跨度的全景影像、无人机现场补拍图。
- 全景影像为**已有产物**：系统每日定时拍摄生成单张全景图，无需研发方新开发调取。

**处理**（研发方实现）：

1. **多时相数据串联**：系统自动抽取时空信息，基于坐标抓取特定地点的前后时间线图片，自动完成时序对比。
2. **多媒体质量评估智能体嵌入**：构建指标体系，对每张候选照片评估清晰度、光照、目标完整性、遮挡程度、时间有效性，标注质量评分与可用性标签（"清晰可用"/"光线不足"/"目标遮挡"）。
3. **智能筛选**：系统自动挑选主图/辅助线索图/备选图片——质量最优且最能反映违法事实核心的作**主图**（预警首图）；能补充主图信息、展示事件不同侧面的作**辅助线索图**；质量一般但有参考价值的作**备选图片**（研判补充）。
4. **证据包一键导出**：主图+辅助线索图+时序对比图+相关视频片段打包，供业务方用于执法部门立案。
5. **大模型语义理解与预警文案**：区分路过/作业中的挖掘机、规模（点状/面状）、紧迫程度（持续进行中/已停工）；违法阶段按**业务方提供的清单**判断（如初期/中期/已既成），生成符合执法文书规范的预警判断结果。

**输出**：

- 研发方产出：JSON 文件（图片名 + 质量评分 + 可用性标签），按主图、辅助线索图等维度分组，**不回传原图**。
- 业务方负责：将评估结果**整合成报告**。

**职责分工**（会议确认）：

| 职责       | 研发方                                                     | 业务方                                 |
| ---------- | ---------------------------------------------------------- | -------------------------------------- |
| 数据供给   | —                                                         | 提供带坐标图集、全景影像（均带坐标）   |
| 算法分析   | 指标体系打分打标、多时相串联、智能筛选、语义理解、预警文案 | —                                     |
| 输出       | 本地输出 JSON 评估文件                                     | 整合评估结果为报告                     |
| 集成与展示 | 只管底层分析                                               | 系统联调、结果筛选、业务展示与排版美化 |

**关键决策**：

- 输入简化：直接接收含经纬度和时间的多维度图片，**系统自动抽取时空信息**，自动完成时序对比与主图筛选。
- "辅助线索图"与"时序对比图"概念重叠：前者来源更多样（含无人机现场补拍图），以区分。
- 图片**打标签区分来源**（全景图、无人机图），用标签做数据隔离。
- 全景影像**已存在**（系统每日定时拍摄生成单张全景图），无需额外调取外部数据。
- 输出仅返回**图片名 + 质量标签**，**不回传原图**；底层只做分组"搭骨架"，美化排版由业务端完成。
- 需加入**语义理解**，用自然语言还原事件时间线并给出预警结论。

### 功能二：样本自动挖掘

**输入**：包含目标场景的视频或图片集（综合感知历史检测数据）。

**处理**：

1. **抽帧定位（当前阶段）**：自动识别并抽帧出包含目标的图片，**优先返回图片结果供确认是否找准**。
2. **自动框选（后续推进）**：在抽帧找准的基础上，再标注 **bounding box**。
3. **语义扩展挖掘**：理解业务侧核心标签，利用**大模型语义扩展和关联理解**，打破关键词局限，挖掘更多潜在目标。
4. **候选样本筛选**：基于历史经验规则生成候选样本列表，用**主动学习算法**筛选高价值样本。

**输出**：当前阶段先输出抽帧出的图片结果（供人工确认是否正确）；后续阶段补齐 **bounding box** 标注后，形成可供训练的图片数据集。

**关键决策**：

- 目标**不固定**，但严格基于公司**现有标签清单**识别，非盲目扫描。
- 当前阶段核心诉求是"**找得准"**：先确保抽帧找对目标，再逐步推进自动框选与评估。
- 违法行为清单：业务方**仅提供违法事项大类**，具体表现靠语义理解自行扩散，算法侧兜底。
- 数据规模：**视频量不大**，输入源含视频与图片集，数据量可控。

### 功能三：样本质量评估

**输入**：样本自动挖掘工具生成的图片数据集。

**处理**：检测图像清晰度、光照、遮挡、目标完整性等质量维度；检查标注是否完整、规范；评估样本集类别分布与多样性。

**输出**：综合各项指标的质量评分，供模型训练使用。

**关键决策**：

- 评估与样本挖掘**联动**，直接剔除低质数据、留存高质量样本，构建数据闭环。

### 功能四：线索报告生成

**输入**：经过质量评估的图集，包含主图、辅助线索图、时序对比图等。

**处理**：基于多图证据，利用大模型 + 变化趋势时序分析算法进行语义理解，还原事件时间线，给出预警结论。

**输出**：JSON 格式线索报告（自然语言描述的事件故事）。

**关键决策**：

- 输出采用 **JSON 或 MD 文件**，仅按主图、辅助线索等维度分组，美化排版交由业务端。

> **横向说明**：照片质量评价（多媒体质量评估智能体）作为独立能力，自动评估抓拍图/预警照片的清晰度、光照、遮挡、目标完整性、时间有效性，输出质量标签、可用性建议与问题描述。其已嵌入"线索智能化生成"流程，也可独立支撑样本质量评估等环节。

---

## 二、分工

| 方                   | 职责                                                                                                                            |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 业务方（王久玲）     | 提供带坐标图集（图片名 + 经纬度）、全景影像、违法阶段清单，补充数据与样本；整合评估结果为报告；负责系统联调、结果筛选与业务展示 |
| 研发方（吕建明团队） | 构建指标体系（清晰度、光照度等打分打标），负责底层分析，输出 JSON 评估结果                                                      |

---

## 三、数据接口约定

| 项       | 约定                                                                    |
| -------- | ----------------------------------------------------------------------- |
| 服务形式 | 工具以**接口形式**被平台调用                                      |
| 数据交互 | 通过**指定目录**交换数据                                          |
| 输出格式 | 轻量**JSON**（含图片名 + 质量标签），不回传原图；报告类用 JSON/MD |
| 耦合度   | 用轻量 JSON 交互，降低系统耦合                                          |

---

## 四、待办

| # | 事项                           | 负责人 |
| - | ------------------------------ | ------ |
| 1 | 需求文档确认（与王总）         | 吕建明 |
| 2 | 数据与样本补充（确认后供调试） | 王久玲 |
| 3 | 提供违法阶段清单               | 王久玲 |
